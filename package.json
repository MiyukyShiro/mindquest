{
  "name": "mindquest",
  "version": "1.0.0",
  "private": true,
  "description": "MindQuest — The Sound of My Mind",
  "scripts": {
    "setup": "npm i @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest",
    "add:android": "npx cap add android",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  }
}
